# v0.1.0 ships as a pure-Dart package. The following are either dev-only,
# pending-for-v0.2.0, or too large for pub.dev.

# Bundled node_modules (≈157MB). Consumers install quicktype via
# `npm install -g quicktype` or rely on `dart run build_runner`.
tool/

# Platforms not yet wired up for the FFI plugin — each lands in a
# subsequent v0.2.0-dev.N release.
android/
ios/
windows/
web/

# IDE / tool caches
.dart_tool/
.idea/
.vscode/
build/

# OS clutter
.DS_Store

# Local smoke test (not part of the public surface)
bin/smoke.dart
