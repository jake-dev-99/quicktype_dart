// To parse this JSON data, do
//
//     final package = packageFromJson(jsonString);

import 'dart:convert';

Package packageFromJson(String str) => Package.fromJson(json.decode(str));

String packageToJson(Package data) => json.encode(data.toJson());

class Package {
    String name;
    String description;
    String version;
    Repository repository;
    String homepage;
    String main;
    Scripts scripts;
    DevDependencies devDependencies;
    List<String> keywords;
    Author author;
    String license;
    Testling testling;

    Package({
        required this.name,
        required this.description,
        required this.version,
        required this.repository,
        required this.homepage,
        required this.main,
        required this.scripts,
        required this.devDependencies,
        required this.keywords,
        required this.author,
        required this.license,
        required this.testling,
    });

    factory Package.fromJson(Map<String, dynamic> json) => Package(
        name: json["name"],
        description: json["description"],
        version: json["version"],
        repository: Repository.fromJson(json["repository"]),
        homepage: json["homepage"],
        main: json["main"],
        scripts: Scripts.fromJson(json["scripts"]),
        devDependencies: DevDependencies.fromJson(json["devDependencies"]),
        keywords: List<String>.from(json["keywords"].map((x) => x)),
        author: Author.fromJson(json["author"]),
        license: json["license"],
        testling: Testling.fromJson(json["testling"]),
    );

    Map<String, dynamic> toJson() => {
        "name": name,
        "description": description,
        "version": version,
        "repository": repository.toJson(),
        "homepage": homepage,
        "main": main,
        "scripts": scripts.toJson(),
        "devDependencies": devDependencies.toJson(),
        "keywords": List<dynamic>.from(keywords.map((x) => x)),
        "author": author.toJson(),
        "license": license,
        "testling": testling.toJson(),
    };
}

class Author {
    String name;
    String email;
    String url;

    Author({
        required this.name,
        required this.email,
        required this.url,
    });

    factory Author.fromJson(Map<String, dynamic> json) => Author(
        name: json["name"],
        email: json["email"],
        url: json["url"],
    );

    Map<String, dynamic> toJson() => {
        "name": name,
        "email": email,
        "url": url,
    };
}

class DevDependencies {
    String matcha;
    String tape;

    DevDependencies({
        required this.matcha,
        required this.tape,
    });

    factory DevDependencies.fromJson(Map<String, dynamic> json) => DevDependencies(
        matcha: json["matcha"],
        tape: json["tape"],
    );

    Map<String, dynamic> toJson() => {
        "matcha": matcha,
        "tape": tape,
    };
}

class Repository {
    String type;
    String url;

    Repository({
        required this.type,
        required this.url,
    });

    factory Repository.fromJson(Map<String, dynamic> json) => Repository(
        type: json["type"],
        url: json["url"],
    );

    Map<String, dynamic> toJson() => {
        "type": type,
        "url": url,
    };
}

class Scripts {
    String test;
    String bench;

    Scripts({
        required this.test,
        required this.bench,
    });

    factory Scripts.fromJson(Map<String, dynamic> json) => Scripts(
        test: json["test"],
        bench: json["bench"],
    );

    Map<String, dynamic> toJson() => {
        "test": test,
        "bench": bench,
    };
}

class Testling {
    String files;
    List<String> browsers;

    Testling({
        required this.files,
        required this.browsers,
    });

    factory Testling.fromJson(Map<String, dynamic> json) => Testling(
        files: json["files"],
        browsers: List<String>.from(json["browsers"].map((x) => x)),
    );

    Map<String, dynamic> toJson() => {
        "files": files,
        "browsers": List<dynamic>.from(browsers.map((x) => x)),
    };
}
